'use strict';

/**
 * @ngdoc function
 * @name webApp.controller:UnionCtrl
 * @description
 * # UnionCtrl
 * Controller of the webApp
 */
angular.module('webApp')
  .controller('UnionCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
